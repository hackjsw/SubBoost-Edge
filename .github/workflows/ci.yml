name: CI

on:
  workflow_dispatch:
  pull_request:
  push:
    branches:
      - main
      - "ryan/**"

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-node@v6
        with:
          node-version: "22.19.0"
      - run: npm ci
      - run: npm run lint
      - run: npm run test:unit
      - run: npx tsc --noEmit -p packages/core/tsconfig.typecheck.json
      - run: npx tsc --noEmit -p packages/server-core/tsconfig.typecheck.json
      - run: npx tsc --noEmit -p packages/ui/tsconfig.typecheck.json
      - run: npm run edge:typecheck
      - run: npm run edge:build
      - run: npm run check:local-app
